# 内容引擎使用说明

内容引擎不是课堂里突然生成剧情的 AI。它是一个稳定的发布闭环：

**飞书改内容 → 自动校验并发布 → Godot 关卡变化 → 试玩证据回到飞书。**

## 你实际只做四步

1. 打开[内容引擎飞书 Base](https://hcn3j6gp4127.feishu.cn/base/A7IcbT6oHaUKXIsAjHrcrlhinwg)，进入“内容单元”。
2. 找到“数据花园：用循环唤醒世界”，升级“版本”，再修改“主任务目标数”或“迁移目标数”。目标数允许 2—8。
3. 勾选“发布请求”。如果 Demo 没开，双击 `发布内容引擎.bat`；如果 Demo 已开，后台每 3 秒会自动检查。
4. 等“发布结果”显示成功，再重新进入 Demo。画面顶部会直接显示内容版本、两个目标数和哈希。

示例：把版本从 `0.5.0` 改为 `0.6.0`，把“迁移目标数”从 `6` 改为 `7`，勾选发布。新关卡就会绘制 7 个巡检目标，正确循环次数也会变成 7；小核桃仍按同一套五级提示规则工作。

## 飞书里哪些东西会影响游戏

| 位置 | 你改什么 | 游戏里发生什么 |
|---|---|---|
| 内容单元 | 主任务目标数 | 光种数量、正确次数、反事实实验、C 代码与界面文案一起变化 |
| 内容单元 | 迁移目标数 | 巡检目标数量、正确次数、轨迹与迁移反馈一起变化 |
| 提示策略 | 教学意图、提示模板、是否允许答案 | 小核桃五级提示随发布版本变化；前四级会做泄题校验 |
| 内容单元 | 版本、发布请求 | 触发不可变内容包、哈希和 Godot/AI 双端同步 |
| 课堂证据 | 无需手改 | 一局结束后自动出现效果、趣味代理和关键学习证据 |

提示模板中可以写 `{main_target}`。例如“完整结构是重复 `{main_target}` 次”，发布时会自动替换为当前主任务目标数。

## 怎么判断发布是否真的生效

- 飞书“发布请求”自动恢复为未勾选。
- “发布结果”显示版本、主任务数、迁移数和哈希前 12 位。
- “自动测试通过”为勾选状态。
- Godot 画面顶部显示相同版本与哈希前 8 位。
- 完成一局后，“课堂证据”出现相同内容版本和完整哈希。

如果发布失败，系统不会替换上一个可用关卡；原因会写在“发布结果”里。最常见的原因是没有升级版本号、目标数超出 2—8、五级提示不完整，或前四级提示泄露了正确次数。

## AI 世界工坊

在“内容单元”选择“主题”并勾选“生成请求”。Doubao Seed 2.0 Pro 生成受限文案，Seedream 5.0 lite 生图；候选通过文案白名单、素材检查、去重和 Godot 试玩后，Doubao Pro 会真实读取预览，检查主题、前后构图、实体/动作、精灵底色、画风与儿童安全。blocking 问题只重生成受影响素材，复检通过后预览才会回到“候选预览”。确认后同时勾选“候选审核通过”“安全审核通过”“发布请求”。发布器会再次要求视觉质检已通过；发布成功会清空候选路径，素材哈希进入总内容哈希。详细命令与变式回访流程见 [AI世界工坊与迁移变式使用说明.md](./AI世界工坊与迁移变式使用说明.md)。
